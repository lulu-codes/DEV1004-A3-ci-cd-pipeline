# Backend .dockerignore file - Excludes files from Docker build context
# (For faster builds, smaller image and better security)

# Ignore dependencies
# node modules - as they will be installed fresh in build
node_modules
yarn.lock
.yarn/
.pnp.*

# Ignore log files
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignore environment variable files
.env
.env*

# Ignore version control git files
.git
.gitignore
.gitattributes

# Ignore IDE files and folders
.vscode

# Ignore build artifacts
dist/
build/
coverage/   # Old coverage excluded, new coverage will be generated in container
.nyc_output/
*.coverage

# Ignore CICD files
.github/

# Docker files - prevent recursion issues and shouldn't be in the image itself
Dockerfile*
docker-compose*.yml
.dockerignore

# OS-specific files - shouldn't be in containers
.DS_Store        # macOS
Thumbs.db        # Windows
desktop.ini      # Windows